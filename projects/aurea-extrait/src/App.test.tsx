import {render,screen} from '@testing-library/react';import '@testing-library/jest-dom/vitest';import {describe,it,expect} from 'vitest';import App from './App';describe('Aurea campaign',()=>{it('renders its manifesto and demo disclosure',()=>{render(<App/>);expect(screen.getByRole('heading',{name:'AUREA',level:1})).toBeInTheDocument();expect(screen.getByText(/no payment will be processed/i)).toBeInTheDocument()})});
