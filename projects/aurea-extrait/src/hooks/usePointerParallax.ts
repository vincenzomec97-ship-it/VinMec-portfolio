import {useState} from 'react'; import {useReducedMotion} from './useReducedMotion'; export function usePointerParallax(){const reduced=useReducedMotion(); const [point,setPoint]=useState({x:0,y:0}); return {point,onPointerMove:(e:React.PointerEvent<HTMLElement>)=>{if(reduced||e.pointerType==='touch')return; const r=e.currentTarget.getBoundingClientRect(); setPoint({x:((e.clientX-r.left)/r.width-.5)*10,y:((e.clientY-r.top)/r.height-.5)*8})},onPointerLeave:()=>setPoint({x:0,y:0})};}
