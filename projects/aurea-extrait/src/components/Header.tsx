import {useEffect,useState} from 'react'; import {Logo} from './Logo'; import {nav} from '../data/fragrance';
export function Header(){const [open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false); useEffect(()=>{const f=()=>setScrolled(scrollY>24); addEventListener('scroll',f,{passive:true});return()=>removeEventListener('scroll',f)},[]); useEffect(()=>{document.body.classList.toggle('locked',open); const key=(e:KeyboardEvent)=>e.key==='Escape'&&setOpen(false);addEventListener('keydown',key);return()=>{removeEventListener('keydown',key);document.body.classList.remove('locked')}},[open]); return <header className={`header ${scrolled?'is-scrolled':''}`}><a href="#fragrance" aria-label="Aurea home"><Logo/></a><nav id="main-menu" className={open?'is-open':''} aria-label="Navigazione principale">{nav.map(([label,id])=><a key={id} href={`#${id}`} onClick={()=>setOpen(false)}>{label}</a>)}<a className="nav-cta" href="#edition" onClick={()=>setOpen(false)}>Discover Aurea</a></nav><button className="menu-toggle" aria-expanded={open} aria-controls="main-menu" aria-label={open?'Chiudi menu':'Apri menu'} onClick={()=>setOpen(!open)}><span/><span/></button></header>}
