import {motion} from 'framer-motion'; import {notes} from '../data/fragrance'; export function FragranceNotes(){return <section className="notes" id="notes"><header><span className="eyebrow">Olfactive architecture / 01—03</span><h2>BUILT IN<br/>LAYERS.</h2></header><div className="note-grid">{notes.map((n,i)=><motion.article className={`note note--${n.tone}`} key={n.number} initial={{y:50}} whileInView={{y:0}} viewport={{once:true}} transition={{delay:i*.12}}><strong>{n.number}</strong><div><p>{n.phase}</p><h3>{n.items.map(x=><span key={x}>{x}</span>)}</h3><small>{n.text}</small></div></motion.article>)}</div></section>}
